// The pinned upstream client ABI still exports legacy symbol names. Keep that detail in one internal adapter.
export { OpenCode as SwiftCoder, type OpenCodeClient as SwiftCoderClient } from "@opencode-ai/client/promise"
export { OpenCode as SwiftCoderHealthClient } from "@opencode-ai/client"
